apiVersion: v1
kind: Pod
metadata:
  name: pod-config
spec:
  containers:
    - name: nginx
      image: nginx
      env:
      - name: course
        valueFrom:
          configMapKeyRef:
            name: aravind123-web
            key: course
      - name: trainer
        valueFrom:
          configMapKeyRef:
            name: aravind123-web
            key: trainer
      - name: githublink
        valueFrom:
          configMapKeyRef:
            name: aravind123-web
            key: githublink
      
