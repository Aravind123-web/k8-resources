apiVersion: v1
kind: Pod
metadata:
  name: pod-secrets
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom: # to import all the key-value pairs from secret as environment variables
      - secretRef:
          name: pod-secret